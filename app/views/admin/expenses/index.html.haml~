%h1 All Expenses

%table#expenses
  %thead
    %tr
      %th Incurred Date
      %th Amount
      %th Category
      %th Remarks
      %th More Info
  %tbody
    - @expenses.each do |expense|
      %tr
        %td= expense.incurred_date.strftime("%d %B, %Y") 
        %td= expense.amount
        %td= expense.category
        %td= expense.remarks
        %td= link_to "Treatment of #{expense.amount}", admin_expense_path(expense)

= link_to 'Add new expense', new_expense_path
